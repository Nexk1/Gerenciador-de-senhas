#import bcrypt as bc
#class cript_senha():
    #def __init__(passwordi, checkinp):
        #password = passwordi
        #check = checkinp
        #password = password.encode('utf-8')
        #hashed = bc.hashpw(password, bc.gensalt(10))
        #print(hashed)

        #check = check.encode('utf-8')
        #print(check)

        #if bc.checkpw(check, hashed):
        #    pass
        #else:
        #    return
